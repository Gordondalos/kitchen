import { HideNotShopPipe } from './hide-not-shop.pipe';

describe('HideNotShopPipe', () => {
  it('create an instance', () => {
    const pipe = new HideNotShopPipe();
    expect(pipe).toBeTruthy();
  });
});
